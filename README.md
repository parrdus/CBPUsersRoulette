# CBPUsersRoulette
Пользовательское действие (активити) для бизнес-процессов Bitrix, которое выбирает случайного пользователя из заданного списка или отделов с поддержкой исключений.

## 📋 Описание

**Users Roulette** — это активность для конструктора бизнес-процессов, которая:

- ✅ Выбирает случайного пользователя из явного списка ID
- ✅ Автоматически собирает пользователей из указанных отделов (включая подотделы)
- ✅ Исключает заданных пользователей из выбора
- ✅ Возвращает резервного пользователя, если кандидатов не найдено
- ✅ Ведёт подробный лог выполнения

**Возвращаемые значения:**
- `User` — выбранный пользователь (формат `user_ID`)
- `LogMessage` — текстовый лог выполнения

## 🚀 Установка

1. Скопируйте папку `usersroulette` в директорию:
   ```
   /local/activities/
   ```

2. Убедитесь, что права на файлы позволяют чтение PHP-файлов

3. Очистите кеш Bitrix (опционально):
   - Настройки → Настройки продукта → Автокеширование → Очистка файлов кеша

4. Действие появится в конструкторе БП в категории **"Пользовательские активити"** (или "Custom activity")

## 📖 Использование

### Параметры действия

| Параметр | Обязательный | Описание |
|----------|:------------:|----------|
| **Пользователь по умолчанию** | ✅ | ID пользователя, который будет выбран, если пул кандидатов пуст |
| **Список ID пользователей** | ❌ | Конкретные ID пользователей для участия в розыгрыше (через запятую) |
| **Список ID департаментов** | ❌ | ID отделов, из которых автоматически собираются пользователи (включая подотделы) |
| **Список ID для исключения** | ❌ | ID пользователей, которых всегда исключаем из выбора |

### Примеры настройки

**Пример 1:** Выбор из конкретных пользователей
```
Пользователь по умолчанию: 1
Список ID пользователей: 5, 12, 34, 67
```

**Пример 2:** Выбор из отдела IT (ID=3) с исключением руководителя (ID=15)
```
Пользователь по умолчанию: 1
Список ID департаментов: 3
Список ID для исключения: 15
```

**Пример 3:** Комбинированный выбор
```
Пользователь по умолчанию: 1
Список ID пользователей: 45, 67
Список ID департаментов: 5, 8
Список ID для исключения: 1, 2, 3
```

### Скриншоты

*(Здесь разместите скриншоты)*

**Скриншот 1:** Настройка параметров действия в конструкторе БП  
*Описание: Форма с полями для ввода Default User, Users List, Dept List, Forbidden Users*

**Скриншот 2:** Пример использования в бизнес-процессе  
*Описание: Действие "Рулетка пользователей" в цепочке БП*

**Скриншот 3:** Лог выполнения  
*Описание: Пример вывода LogMessage с информацией о выбранном пользователе*

## 🔧 Технические детали

### Зависимости
- Bitrix (версия с поддержкой D7)
- Модуль `iblock` (для работы со структурой компании)
- Инфоблок структуры компании (`intranet/iblock_structure`)

### Как это работает

1. Парсит списки ID из строковых параметров
2. Собирает пользователей из явного списка (если указан)
3. Находит все подотделы указанных департаментов (через nested sets)
4. Собирает активных пользователей из найденных отделов
5. Исключает запрещённых пользователей
6. Случайным образом выбирает одного из оставшихся кандидатов
7. Если кандидатов нет — возвращает пользователя по умолчанию

### Логирование

Активность генерирует подробный лог:
```
time: 09.05.2026 14:30:15
Пользователь по умолчанию: user_1
Явный список: count{3} = Иванов Иван {5}, Петров Петр {12}
Департаменты определены: {3, 7, 12}
Пользователи из департаментов: count{15} = ...
Исключённые пользователи: count{2} = Сидоров Сидор {89}
Итоговый пул: count{16} = ...
Выбран: Иванов Иван {5} из 16 кандидатов
```

Для записи полного лога в файл раскомментируйте строки:
```php
//$this->logDebug($this->LogMessage);
//$this->logDebug($result['fullLog']);
```
